# Frontend

The frontend is a Next.js App Router application. The initial interface is deliberately simple and establishes routes for the dashboard, jobs, applications, profile, resume, and settings.

Run `npm install` once, then `npm run dev`.
