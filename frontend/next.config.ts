/**
 * Holds process-level Next.js configuration for the frontend application.
 * Feature behavior should remain in application modules rather than this file.
 */
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
